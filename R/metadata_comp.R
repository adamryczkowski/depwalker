#Comparisons between metadata.


# Compares metadatas and asserts, whether the two are different enough
# that there is no longer sense in shared statistics.
are_metadatas_fundamentally_different<-function(metadata1, metadata2) {

}
